# WKEY Take-Home Assignment
Generated by [Rojo](https://github.com/rojo-rbx/rojo) 7.6.1.

## Getting Started
To build the place from scratch, use:

```bash
rojo build -o "WKEY Take-Home Assignment.rbxlx"
```

Next, open `WKEY Take-Home Assignment.rbxlx` in Roblox Studio and start the Rojo server:

```bash
rojo serve
```

For more help, check out [the Rojo documentation](https://rojo.space/docs).


You dont need to run pesde or install it since i didnt add it to the .gitignore this time

USED LIBRARIES:

Knit;
Vide

How it was made:

Used a simple config (spawnAreasConfigs.luau), could be a more complex registry but since its a simple test assignment i made it simple,
made classes for spawn areas and enemies, so its more readable and manageable in the future, used services for better control over the classes.